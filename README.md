# 🛡️ Cloud SIEM Log Automation

본 저장소는 인프라 및 클라우드 환경에서 발생하는 보안 로그(Auth Log)를 실시간으로 분석하여, 무단 접근 시도(Brute Force Attack) 및 이상 징후를 탐지·시각화하는 자동화 SIEM 시스템입니다.

## 🛠️ 핵심 기능 (브랜치 작업 중)
* **feature/log-parser:** 리눅스/클라우드 접근 로그 정규식 파싱 엔진 구현
